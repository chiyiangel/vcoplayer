# VCO Player Web

Mobile-first Operator Remote for the VCO Player MVP.

## Development

Run the Swift backend first, then start the Vite dev server:

```sh
npm install
npm run dev
```

The dev server proxies `/api/*` to `http://127.0.0.1:8080`.

## Verification

```sh
npm test
npm run build
```
